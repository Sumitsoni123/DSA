#include <bits/stdc++.h>
using namespace std;


int main() {
    
    int t;
    cin >> t;

    while(t--)
    {
        int n, i;
        cin>>n;
        
        for(i=2; i*i<=n; i++)
        {
            if(n%i == 0)
                break;
        }
        
        if(i*i > n || n == 1)
            cout<<"prime"<<endl;
        else
            cout<<"not prime"<<endl;
    }
    
}
